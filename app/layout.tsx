import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'GRDN AI - AI-Powered Agriculture Revolution',
  description: 'Transform your farming with AI-powered crop diagnosis, weather forecasts, and market insights. Speak your dialect, grow your future.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}